server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: identity-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/pera_identity}
    username: ${DB_USER:pera}
    password: ${DB_PASSWORD:pera_dev_password}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc.time_zone: UTC
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 3MB

pera:
  jwt:
    secret: ${PERA_JWT_SECRET:change-me-development-secret-at-least-32-bytes}
    issuer: ${PERA_JWT_ISSUER:pera-erp}
    ttl: ${PERA_JWT_TTL:PT8H}
  bootstrap:
    admin-password: ${PERA_BOOTSTRAP_ADMIN_PASSWORD:ChangeMe123!}
  company-logo:
    storage-root: ${PERA_COMPANY_LOGO_STORAGE_ROOT:.runtime/company-logos}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
